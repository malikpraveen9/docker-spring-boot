apiVersion: v1
kind: Service
metadata:
    name: malikpraveendockerspringboot
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: malikpraveendockerspringboot